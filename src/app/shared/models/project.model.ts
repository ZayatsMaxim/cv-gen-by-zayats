export type ProjectModel = {
    projectName: string
    description: string
    startDate: string
    endDate: string
    teamSize: number
    techStack: string[]
    responsibilities: string[]
    teamRoles: string[]
}
